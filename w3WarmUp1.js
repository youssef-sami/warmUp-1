// 1-Create a third variable called z, assign x + y to it, and display it.
	var x = 5;
	var y = 10;
	var z = x + y;
	console.log(z)
// 2-calculate the area of a square using varaiables.
function areaOfSquare (side) {
	//the area of a square equals double of the entered side
	return 'area of the square equales '+ side*side;
}
