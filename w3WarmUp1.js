// 1-Create a third variable called z, assign x + y to it, and display it.
	var x = 5;
	var y = 10;
	var z = x + y;
// 2-calculate the area of a square using varaiables.


function displaySum(sum){
	return z;
}

// after assigning a value for z which is the sum of the rest of the values (x,y) the fuction returns and displays the value of z

function areaOfSquare(side){
	var area = side * side;
	return area;
}


// this function takes the length of the side of the square as a parameter and then computes the area by multiplying the side by itself and returning the result which is the area of the given square
