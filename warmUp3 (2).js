function myTrueFun(){
	return true;
}

// this fuction returns true everytime it's called or invoked because it has return true 

function typeFunc(value){
	if(typeof value === "string"){
		return true;
	} else {
		return false;
	}
    }

    // this function returns true if the parameter is aa string through using an if statement and false otherwise


function primeOrNot(num){
	if (num>1 && num/num)
}


// this function should return true if the num that was entered as a parameter is greater than 1 and can only be divided by 1 and itself

function equal (num1 , num2){
	if(num1 === num2){
		return true;
	} else {
		return false;
	}
}

// this function returns true if it's parameters are equal and false if (through using an if statement) they are not so if num1 and num2 are 5 so true and if num1 is 5 and num2 is 6 then it will return false